---
name: h3-video-editing-prompt
description: MiniMax H3 Ref2VA 视频编辑提示词中文速查，重点覆盖蒙版换人、换脸与口型保留。
---

# H3 视频编辑提示词：中文速查

权威执行入口是 `SKILL.md`；本文件只保留中文说明，不重复大量案例。

## 核心原则

- 写目标画面，不写后期操作命令。
- 参考图负责外观；源视频帧负责姿势、动作、表情、视线和可见嘴形；源场景负责光照、透视、遮挡与接触关系。
- 源视频音轨或独立参考音频只有在真实存在、与画面同时间轴且确实送入生成节点时，才能成为口型校验信号。
- 提示词不能硬锁区域、时间窗或逐帧口型。成片已经漂移时，不能继续承诺“改提示词就能精确修复”。

## 写之前必须确认

1. 视频时长、fps、帧数、分辨率、镜头切点。
2. 源视频是否有音轨，或是否另有独立音频。
3. 生成节点是否真的接收到与画面同时间轴的目标音频；“提供了音频”和“节点收到且对齐”不是一回事。
4. 遮罩从哪一帧开始、覆盖到哪里，是半透明、不透明，还是保留内部结构的反相／伪彩覆盖。
5. 嘴唇轮廓、嘴角、下颌、牙齿、舌头和闭口帧是否仍可见。
6. 遮挡物、手持道具、画外身体部位和其他人物。

## 六字段

严格按以下顺序输出：

```text
subject_definitions:
summary:
retention_analysis:
detailed_description:
overall_soundscape:
non_diegetic_music:
```

只用 `<Subject N>`、`<Picture N>`、`<Video N>`、`<Audio N>`。禁止输出 `<Theme N>`、`<Topic N>`、`Topic definition`、`Task`、`Retain analysis`。

成稿目标约 7000 字符，硬性不得超过 10000 字符，交付前实际计数。

## 蒙版换人归属

- 遮罩内的外观全部按参考图重建；源人物在遮罩内的发型、帽子、衣服和配饰不能同时写成“保留”。
- 遮罩外的身体和物件逐项保留。
- 可见动作、体位、时序、接触和深度顺序仍归源视频。
- 多人物画面要单独写明其他人物的外观、位置、动作、时序与前后关系不变。
- 遮罩半途出现时，未标记段不进 H3；只生成标记段再拼回。不能靠一句“前半段不变”实现。

## 反相／伪彩整人遮罩

如果目标整个人变成蓝绿、负片或其他伪彩，但五官、嘴形、身体轮廓、衣褶和动作仍然可读，按“**外观被污染、几何仍可用**”处理，不按纯色空白处理：

- 源帧继续负责轮廓、姿势、动作、表情、眼神、嘴形、接触、裁切和遮挡。
- 遮罩颜色、皮肤、头发、服装、材质和纹理全部不保留；目标外观全部来自参考图。
- 成稿只需集中写一次：不得残留绿、青、蓝、负片色、透明感、色边、硬块、双轮廓、源特征泄漏、重影或闪烁。
- 光照与透视来自源场景，但伪彩颜色和参考图影棚光都不迁移。

`<Subject 1>` 必须定义成跨镜头持续存在的同一个目标角色，不能只写成一个匿名区域：

```text
<Subject 1> is the same marked target character wherever any part of that character appears in <Video 1>, including full, partial, cropped, edge-of-frame, foreground, and post-cut appearances.
```

每个实测切点都必须新建 `[Shot N] At MM:SS.mmm`，并再次说明该镜头中 `<Subject 1>` 的所有可见部分仍使用同一个 `<Subject 2>`：

- 目标局部出画时保持原裁切，不把画外身体拉进来。
- 切镜后只剩边缘前景碎片时，写明位于哪一侧、大小、裁切和前后关系，防止被当成背景。
- 某镜头没有目标时，明确该镜头不出现目标，不要凭空添加。
- 目标无明显遮罩但按连续性确定是同一角色时，才继续替换。

如果跨切镜仍恢复源人物、漏出伪彩或闪烁，停止继续堆提示词；按原硬切点逐镜头生成再拼接。

## Ref 条件视频 + 参考图 + 独立音频

这是当前工作流的默认三输入：

- `<Video 1>`：带颜色蒙版的 Ref 条件视频。蒙版只负责指认目标；可见帧负责动作、姿势、表情、眼神、嘴形、时序、机位、光照、接触和遮挡。
- `<Subject 1>`：从 `<Video 1>` 抽象出的表演与几何载体，不代表要保留其遮罩内外观。
- `<Subject 2>`：参考图中的目标人物或物体，负责遮罩内的完整外观。
- `<Audio 1>`：另外提供的参考音频，只有在角色与时间对齐确认后才使用。

参考图目标先分流：

- **人物或有脸角色**：按参考图重建完整外观；逐时刻复刻 Ref 视频里可见的眉眼、眼周松紧、视线、眨眼、嘴角、头部、姿势、手势、重心、速度、停顿和次级运动。所谓“情绪一致”要落到这些可见变化，不用一个固定情绪词替代整段表演。
- **普通物体**：按参考图重建形状、颜色、纹样、材质和细节；位置、占位、朝向、缩放、速度、路径、接触和遮挡跟随蒙版载体。普通物体不写表情、情绪、眼神、讲话或口型。
- 人形蒙版换成拓扑差异很大的物体时，只保留中心、大小、朝向、运动路径和时序，不宣称物体复刻人体关节或面部动作。

独立音频再分流：

1. 先核对音频时长、起始偏移、静音填充、时间基和声源人物。
2. 音频原信号会直接作为最终声音时，用 `<Audio 1>`、`audio reuse`、`fully_copy`。
3. 只借音色、风格或演唱方式时，用 `audio reference`，不能把它当绝对口型时间轴。
4. 只有声音属于被遮罩人物或有脸角色时才做口型同步。
5. 音频与 Ref 视频可见嘴形同时间轴时，采用“源帧嘴形 + 音频校验”。
6. 音频是新台词或新时间轴时，不能同时要求保留源视频的精确唇形；此时保留源眉眼、头身和动作表演，由最终音频驱动新嘴唇与下颌，并准备下游专用口型重演。

## 口型四分支

### A. 源嘴形可见，且原音轨送入 H3

采用“源帧嘴形 + 原音轨”共同约束：源帧提供上下唇轮廓、嘴角、下颌开合、牙齿与舌头可见度、闭口帧、停顿和头部联动；音轨只校验开闭口、持续音、停顿、呼吸和重音时序。不要把嘴唇控制权完全交给音频，也不要手写音素时间表。

### B. 源嘴形可见，但节点只收到画面帧

只写保留源视频的可见嘴形和时序，不建 `<Audio 1>`，不声称音频驱动同步。生成后重新挂回原音轨；要求精确同步时，追加专用 audio-driven lip-sync 或 face reenactment。

### C. 嘴部被不透明遮罩抹掉，但节点收到音频

音频只能提供近似时序，源视频已无法提供被抹掉的嘴形。不得承诺逐帧准确；精确结果交给下游口型重演。

### D. 没有可用音轨

- 嘴形仍可见：只保留可见嘴形。
- 嘴形也被抹掉：明确没有同步锚点；除非用户提供可靠的台词与对齐数据，否则不编造口型。

## 成片已经对不上时

先拿到成片并保留原音轨，再分类：

- 固定提前或滞后：测量延迟，检查解码、重采样、封装与时间基。
- 局部错形：使用专用音频驱动口型或面部重演。
- 越到后面越偏：检查 fps、帧数、总时长、采样率和变速操作，先修时间轴。

禁止人工按汉字、单词、音节或音素估算时间段，也不要继续承诺仅靠提示词精确修复。

## ComfyUI_Qwen_H3_Prompt

该节点只收到视频帧，不收到音轨：

- 去掉 `audio reuse`、`<Audio 1>`、`fully_copy`；
- 使用上面的 B 或 D 分支；
- 默认写 `overall_soundscape: N/A` 与 `non_diegetic_music: N/A`；
- 不承诺本轮 H3 生成能与原音轨精确同步。

## 最终检查

- 已把参考图目标分成人物/有脸角色或普通物体，普通物体没有情绪和口型句。
- 已分别确认音频存在性、用途、时长、偏移、声源人物和节点音频输入。
- 最终复用信号用 `audio reuse`；只借音色或风格用 `audio reference`。
- 新台词或新时间轴没有同时要求复制源视频的精确唇形。
- 只选择一个口型分支，没有“复制源嘴形”和“嘴形完全跟随音频”的冲突。
- 没有手写音素或字词时间轴。
- 六字段与标签合法。
- 遮罩内外归属正确。
- 反相／伪彩遮罩在结构可读时按“外观污染、几何可用”处理，遮罩颜色与材质未被保留。
- `<Subject 1>` 是跨镜头持续目标，每个切点都有独立 `[Shot N]`，切后局部入镜仍明确继承 `<Subject 2>`。
- 画面边缘裁切保持不变，画外部位没有被拉入画面。
- 时间窗由切段保证，不由提示词保证。
- 字符数已实际检查。
